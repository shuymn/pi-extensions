{
  "name": "pi-extensions",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "lefthook install",
    "lint": "bunx biome check --formatter-enabled=false .",
    "lint:fix": "bunx biome check --write --formatter-enabled=false .",
    "fmt:fix": "bunx biome check --write --linter-enabled=false .",
    "fmt:check": "bunx biome check --linter-enabled=false .",
    "typecheck": "bunx tsc --noEmit",
    "test": "find . -name '*.test.ts' -not -path './node_modules/*' -print0 | xargs -0 -n1 bun test",
    "check:fast": "bun run fmt:check && bun run lint && bun run typecheck",
    "check": "bun run check:fast && bun run test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.2",
    "@earendil-works/pi-ai": "0.81.1",
    "@earendil-works/pi-coding-agent": "0.81.1",
    "@earendil-works/pi-tui": "0.81.1",
    "@types/bun": "1.3.14",
    "@types/node": "26.4.0",
    "lefthook": "2.1.12",
    "typebox": "1.3.25",
    "typescript": "6.0.3"
  },
  "keywords": [
    "pi-package",
    "pi",
    "pi-coding-agent"
  ],
  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "overrides": {
    "@earendil-works/pi-agent-core": "0.81.1",
    "@earendil-works/pi-ai": "0.81.1",
    "@earendil-works/pi-coding-agent": "0.81.1",
    "@earendil-works/pi-tui": "0.81.1"
  },
  "dependencies": {
    "@anthropic-ai/sandbox-runtime": "^0.0.61"
  }
}
