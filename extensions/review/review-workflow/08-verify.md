# Stage 8: Verify

Discover validation commands from project instructions, package scripts, and nearby tests. Run the narrowest relevant formatter/test/typecheck/lint that can verify the applied fixes.

If a check fails, report the failing command, evidence, and whether the failure is caused by the fix or a pre-existing/out-of-scope condition. If no validation is practical, explain why.
