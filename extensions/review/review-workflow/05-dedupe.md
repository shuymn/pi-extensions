# Stage 5: Dedupe

Collapse confirmed or likely actionable findings with the same root cause into one actionable item without modifying files.

Merge findings only when one root fix resolves all variants without hiding distinct impact. Preserve the canonical finding, merged evidence, affected files, duplicate variants, and validation status for Trace. Keep findings separate when they require different fixes or carry meaningfully different impact.
